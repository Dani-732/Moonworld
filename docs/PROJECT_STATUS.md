# MoonWorld 项目进度

> 本文件是项目进度的唯一真源。设计规则仍以 `docs/design` 下的设计文档为准；完成新切片后只在这里更新进度，不再新增第二份进度记录。

## 1. 当前基线

- 更新日期：2026-09-06
- 仓库：`D:\AGENT工作区\rimworld\MoonWorld`
- 分支：`main`
- 已验收功能提交：`817ea9c Support independent servant travel and cross-map actions`，前置为 `ff889c5`；用户反馈“可以了”，确认独立行动与跨图魔力切片，要求先接入依赖中的七名从者，再开始多阵营运行基础。
- 游戏目录：`G:\steam\steamapps\common\RimWorld`
- 测试部署目录：`G:\steam\steamapps\common\RimWorld\Mods\MoonWorld`
- 视觉与角色原型：`D:\AGENT工作区\rimworld\HolyGrailWarTest`
- 当前阶段：第一阶段 MVP、阶段二最小敌方交战、正式敌方突袭及撤退后的场外休整/原从者再袭已由用户确认可用。最小战争胜负、首次召唤准备敌方主从及世界工坊、中文“圣杯战争” Quest 和胜负历史归档均已验收。简易工坊已开放远行队进攻，后续取消主从强制同队离图并补齐灵体撤离，用户已确认本轮验证完毕。当前具备两阵营防守与主动反击路径；自然事件的长期频率、全部跨图组合未逐项验收，不宣称所有 Mod 组合无 bug。多阵营运行时生命周期仍未实现。

### 正式召唤与内容依赖（2026-09-05 已确认）

- 正式御主 Gizmo 与 MoonWorld 开发者入口使用同一召唤服务。
- 新召唤随机使用 `Holy Grail War - Servant Test` 的 `GWW_Artoria` 或 `GWW_Emiya`；原型拥有外观、身体隐藏、礼装锁定及初始装备，MoonWorld 拥有契约、魔力、自治及开战记录。
- 用户确认暂缓内容迁移，授权原型作为依赖，沿用其 HAR 依赖。原型源码和安装文件未修改，更多从者仍在原型中开发，需桥接配置后才进入 MoonWorld 召唤池。
- 旧迁移 `MW_*` Def 与资源仅供旧存档兼容，不再进入新召唤池，不转换为 `GWW_*` 角色；已有己方契约 Guest 的派系按玩家成员迁移规则转换。
- `GameComponent_MoonWorld` 已移至 `Source/Core/WarState.cs`，保留完整类型名及 `warStartTick` 存档键，成功绑定后幂等记录首次开战时间。
- 基线构建、依赖 Def 和补丁目标检查、部署哈希核对通过；用户反馈“可以了，进入下一阶段”，确认本轮游戏内接入可用。没有逐项提供的失败注入证据，不追认成已完成的自动化测试。

## 2. 已完成并验收

### 工程与数据边界

- 新建独立 Mod `MoonWorld`，没有覆盖 `HolyGrailWarTest`。
- 身份与静态数值使用 `ServantIdentityDef`、`ServantResourceProfileDef` 和 `ServantAutonomyProfileDef`。
- 契约关系和存在状态只由 `CompServantState` 保存。
- 御主与从者当前魔力分别由原版 Need 保存，不在组件中复制数值。
- 灵基受损和断供进度分别复用 Hediff severity 与 `ageTicks`。

### 正式开局与事件召唤资格

- 中文剧本“月世界：降灵之兆”沿用原版三人迫降与物资，首位初始角色获得基础回路及学徒位阶；落地后由玩家在邀请信件中指定一名有回路的自由殖民者接受。
- 常规存档通过原版 Incident / ChoiceLetter 接入：基地有合格回路持有者时有机会出现“降灵之兆”，最早第 3 天、基础权重 0.15、同类最短间隔 15 天；普通邀请 3 天到期，剧本首封邀请无到期限制。权重不是每日触发概率。
- 当前只做一届战争。本届事件只给指定接受者授予三划令咒与一次常规召唤资格；不是全殖民地每人一次，也不是角色终身次数。不限制己方从者总数，额外英灵取得方式不在 MVP 内。
- `HolyGrailWarEntry` 由 `GameComponent_MoonWorld.currentWarEntry` 深存，保存指定人和资格是否已使用；角色上不增加终身召唤标记，契约仍唯一保存于从者组件。
- 收到或接受邀请不记录开战；正式原版地图选点与调试入口共用唯一召唤服务，生成、落地、契约绑定全部成功后才消费资格并幂等写入首次 `warStartTick`，不扣令咒划数。
- 取消或失败保留资格；成功后从者湮灭、再次接取、换人或调试入口都不补发。失败清理取消本次契约、销毁 Pawn 并移除世界 Pawn 残留。
- 已开战旧档若缺少事件记录，按本届资格已使用兼容，不改原契约、令咒或开战时间；未开战旧档需通过邀请指定接受者。最小战争胜负已实现；资格转让和下一届尚未实现。

### 御主魔力与供魔

- 魔术回路 Trait 通过 `DefModExtension` 引用回路 Def；魔术师位阶只作身份标签。
- 御主自然回魔、可调整供魔安全线、多从者均分和满魔退出均分池已经实现。
- 实体及灵体从者均可接受御主供魔。
- 从者原版自然饥饿下降关闭；实体化时按 10:1 将食物储备转化为魔力，并在原版进食边界停止。

### 从者身份、生理与自治

- 从者没有睡眠需求，不患疾病，不受衰老、寿命和饥饿归零惩罚。
- 己方契约从者使用 `Faction.OfPlayer` 与空 `HostFaction`；实体允许原版工作、征召、移动和从自身施放能力，保留角色背景与健康的原版限制。
- 顶部头像使用原版成员列表，灵体头像灰显；从者自身命令栏显示魔力和形态命令，御主保留召唤、令咒及形态入口。
- 原版工作、作息、心情、社交和人口规则继续适用；不新增临时成员标记或虚构任务。
- `ServantColonyMembership` 在绑定及完整载入时处理旧 Guest 转换，保护 PawnKind 和魔力，保留工作优先级及旅行 Lord；周期状态校正不再更改派系或移除 Lord。
- 删除 6 个 Guest 专用旅行补丁，使用原版殖民者名单与装载流程；后续 `ea2878f` 已取消全部契约同队验证，保留灵体旅行适配。旧 `LordJob_ServantGuest` 类型保留供反序列化。

### 灵体化与战败

- 主动灵体化可通过御主或从者自身入口调用同一契约校验与生命周期服务；战败可进入战败灵体化。
- 灵体全部人物渲染层为 30% 不透明，普通目标无法索敌。
- 灵体日常跟随同图御主，移动速度为原版速度的 300%；正常距离使用原版寻路，超过阈值时闪现追上。御主未生成、死亡、销毁或不在同图时不跟随、不使用旧坐标闪现；从者有商队集合或登舱安排时旅行优先。
- 灵体停止流血、不能攻击、工作或施放能力；不可站立格不能实体化。
- 普通伤害导致原版倒地或死亡时进入战败；直接 `Pawn.Kill` 不拦截。
- 战败时只最低限度修复致命状态，魔力清零并增加一级灵基受损；伤势仍会导致倒地或死亡时不能实体化。
- 御主死亡后，其未湮灭从者立即湮灭。
- 灵体化立即解除征召并清除优先工作与命令队列；保持可查看状态，禁止普通玩家移动、工作、征召、攻击和施法。

### 简易工坊与主从独立离图

- 世界工坊复用原版 Site 远行队进攻入口及 `GenStep_Outpost` 简单布局，关闭额外普通守军与战利品生成。只安排本届原御主及场外原从者入场，保留契约、装备、伤势、魔力与休整时间；不会把正在突袭或处于其他持有容器的从者拉回工坊。
- 战争未结束时，玩家撤退后保留工坊和存活敌方世界 Pawn；地图卸载再进入可能重建建筑，不保存建筑破坏。工坊内普通战败和建筑被拆不等于战争胜利，死亡/湮灭继续走既有胜负结算。
- 实体与灵体均可与御主分别组建远行队、装舱及离图，不再强制同队。删除 7 个配对拦截补丁，`Pawn.ExitMap` 仅保留敌方离场登记 Postfix。
- 灵体在有原版出口的地图显示“撤离地图”，使用原版出口 Job 独立撤退；御主先从边界离图后，可走出口加入附近可汇合的御主远行队，不跨地图传送。没有可汇合远行队时不会自动追往其他地图。
- 集合出口与登舱行为优先于灵体跟随；显式撤离 Job 不因 AI 再次选 Job 被改回跟随。原版出口、路径、容量等要求仍适用，灵体不搬运旅行物资。
- 没有新增世界地图魔力周期、单独出击额外消耗或跨图形态/宝具权限。空投、穿梭机、重力船进入工坊，多个工坊、敌方逃脱后重建及完整七队仍未实现。

### 测试宝具与令咒

- `测试宝具_魔力爆发` 为角色 Ability，从者自身通过原版选点与施法 Job 释放；40 魔力、30 格射程、1 秒吟唱、3 格爆炸半径、40 Bomb 伤害、300 Tick 冷却，复用原版显眼高爆特效与音效。
- `NoblePhantasmService` 负责有效性、魔力费用、过载和失败回滚；AbilityTracker 保存能力与冷却。当前具体效果仍是 Bomb Explosion，尚未接入原型 Excalibur。
- 御主“宝具过载”消耗一枚令咒，在目标身上保存一次性 Hediff；下一次成功 MoonWorld 宝具免魔力且伤害翻倍，失败/取消保留过载。
- 奇迹重铸清除从者伤势、疤痕及灵基受损；御主令咒数量仍由原组件保存。

### 神秘度与自愈

- 当前白名单只允许实体化英灵造成的攻击伤害实体化英灵；环境伤害可以生效，直接处决不拦截。
- 普通伤口和疤痕按严重度逐点消耗维持线以上的魔力恢复。
- 背痛、虚弱等非伤口有害状态使用原版 `HealthUtility.Cure` 治愈，默认固定消耗 40 点维持线以上的魔力。
- `Hediff_MissingPart`、植入物和 MoonWorld 系统 Hediff 不进入肉体自愈候选。

## 3. 最近验收记录

- `ff889c5` / `817ea9c`：扩展计划已写入 `docs/design/HolyGrailWar_Expansion_Plan.md`。主从同地图、同一远行队或同组旅行运输工具时最低维持线为基础值；分离时默认乘 2，通过原版 Stat 允许特殊能力修正，断供、自愈和显示共用计算。玩家旅行进入统一魔力周期，从者自身宝具和地图内形态切换允许御主异图；令咒过载仍要求同图。用户反馈“可以了”，按整体验收记录。267 项宿主场景、26 个 Harmony 目标与 16 份 XML 检查通过，部署 DLL 哈希已核对；不追认所有第三方旅行容器均测试通过。

- `059f66a Keep summoned servants on map`：修复从者按访客计时离图，加入旧存档 Lord 迁移。
- `163a4a4 Regenerate chronic servant conditions`：加入长期损伤治疗策略及 `conditionCurePranaCost=40`。
- RimWorld 1.6 编译通过。
- 游戏内冒烟测试通过，包括驻留、供魔、灵体化、战败、处决和长期损伤治疗。
- `7e1ad21 Keep contracted servants together during map departure`：同队离图约束实现，离图入口和运输流程复用原版机制；离线 15 项策略测试、构建和部署通过，用户已确认游戏内冒烟测试通过。
- `687f511 Clear servant injuries with miracle recast`：奇迹重铸清除全部原版伤势、疤痕及灵基受损；构建、部署和用户游戏内冒烟测试通过。
- `e065b04` / `7564c43`：测试宝具及原御主选点入口修复已由用户反馈“测试通过”；迁移后该转呈入口被原版从者 Ability 入口替代。
- `970eb53` / `a33232b` / `ae79cc9`：玩家成员身份、旧档迁移、自身 UI、灵体限制与旅行适配分三批完成；用户反馈“目前的验收没发现问题，有问题之后再说”，接受当前迁移并继续下一项。没有逐项反馈的边界不追认为逐项人工通过。
- 迁移构建、84 项宿主替身场景、30 个本机 Harmony 目标与注入参数、11 个 XML 检查通过；29 个部署运行文件 SHA256 一致。静态及替身验证不等价于完整 Unity/AI/渲染验收。
- `679010b` / `0f08b5c`：中文剧本、常规存档邀请、事件指定人及一次召唤资格、原版选点、失败清理与旧档兼容完成；用户于 2026-09-05 反馈“验收无误”，确认本切片，第一阶段 MVP 收尾完成。
- 本切片构建及部署通过；新增 37 项资格/召唤替身场景与原有 84 项回归通过，30 个 Harmony 目标、4 个原版事件/剧本/存档类型和 12 个 XML 检查通过，30 个部署运行文件 SHA256 一致。真实验收是用户整体确认，不将替身存档往返等同于真实 Scribe 验证，也不追认随机事件长期频率及任意第三方异常组合已逐项测试。
- `d39d950` / `d953c2f`：阶段二最小敌方交战切片完成：敌方突袭只部署从者，御主作为场外 WorldPawn 留守；敌方从者优先索敌敌对从者，无有效从者目标才回退到御主及其他人；固定 240/天供魔、原版 Lord 进攻/撤退、战败灵体和场外契约宝具可用。用户已反馈“验收通过”。
- `3a8199b` / `770d382`：修复灵体跟随期间原版近战查询产生的 `has no available melee attack` 日志错误，补齐 MoonWorld Def 配置错误；用户已反馈“验收通过”。灵体近战 8 项、运行时合同 32 项、构建部署及 31 个运行文件 SHA256 检查通过。
- `8bc578b`：修复敌方从者离场后场外魔力不增长、休整倒计时被重置及到期无法再次出击；休整起点改由本届战争条目保存，原版 WorldPawn 只做保留不重复交接。用户已完成游戏内验收。

- `87edd06`：当前两阵营的最小胜负判定由用户确认验收无误。敌方御主死亡或敌方从者死亡/湮灭判定玩家胜利，指定玩家御主死亡判定失败；普通撤退和战败灵体不代表淘汰，结束结果幂等保存，结束后不再突袭。实际代码在 `GameComponent_MoonWorld` 保存 `warOutcome`，尚未创建战争 Quest，未来多阵营设计中的 Quest 归属不能视为当前实现。此验收不包括本次新要求的“首召时生成敌方主从与 Site”。

- `d2ed2ac` / `d428446`：首次成功召唤时准备敌方御主、从者及不可进入的世界工坊，首次突袭部署原从者；用户已确认验收通过，记录提交为 `d4a9480`。
- `2e0bccf` / `791ef46`：首次成功召唤后创建可见、已接受的中文“圣杯战争” Quest；保存开战 Tick 与当前两阵营的御主、从者和工坊引用。用户报告读档后任务消失，日志明确为 `1 quest(s) had null roots after loading`；修复后用户于 2026-09-06 反馈“验收通过”。构建、97 项宿主替身场景、32 个 Harmony 目标及 14 个 XML 检查通过，已部署。真实读档修复由用户整体确认，不将替身测试等同于原版 Scribe 的完整覆盖。
- `c7f50ec`：使用专用 `MW_HolyGrailWarQuest` 根定义替换借用的流浪者根，避免无关慈善事件配置；新任务通过 `Quest.MakeRaw()` 分配原版唯一 ID，旧任务保留身份。胜利/失败调用原版 `Quest.End`，通知一次并显示历史结果；已结束战争的旧档静默同步，不重复通知。用户于 2026-09-06 反馈“验收完成”。构建、104 项宿主场景、32 个 Harmony 目标、15 个 XML 检查通过，部署 DLL 与新增 Quest Def 哈希一致。
- 当前 `QuestPart_HolyGrailWar` 仍保存初始化快照。战争胜负、召唤资格及敌方休整仍由既有 GameComponent / Entry 管理，Quest 负责原版任务状态与历史显示；阵营资格快照尚未接入实时更新。不能把当前任务接入视为多阵营生命周期已完成。
- `315b94a` / `ea2878f`：开放简易魔术工坊及主动反击路径；用户实测发现主从同队离图限制阻塞边界撤退，随后明确要求实体与灵体全部取消限制，并防止灵体闪现异常。修正后用户反馈“验证完毕”，本轮按整体确认记录，不追认每一种地图、运输或真实读档组合均已逐项测试。
- 用户随后明确补充“主动进攻和撤离再战都没问题”，确认这两条真实游戏路径通过；不再要求重复验收。任务胜负归档沿用此前已确认的结论，本次反馈不扩写为所有跨图及读档组合均已验证。
- 本轮构建、离图/灵体旅行 25 项、敌方索敌/撤退 28 项、召唤/战争/工坊 113 项，共 166 项宿主替身场景通过；25 个 Harmony 目标及 15 份 XML 检查通过。部署 DLL SHA256 为 `EC2C6C436209AC75E68E57C7F89FF08EE2ACB07564D50479F36F0CCA7370427A`，仓库与测试部署一致。自动测试不执行 Unity 寻路、实际闪现或真实 Scribe。

## 4. 当前模块化审查

正式突袭追加记录：`12b9a50` 使用原版 Incident，仅在指定御主位于玩家基地、本届尚未部署敌方时触发；最早第 8 天、基础权重 0.08、同类最短间隔 10 天，权重不是每日概率。中文直接入口“执行正式敌方从者突袭事件”调用同一 Worker。用户提供的突袭日志包含深度性能分析输出，未定位到该执行链的异常；用户随后要求进入下一项，不将此视为自然触发频率或所有第三方组合已验证。

身份迁移已验收。成员转换位于 `Lifecycle/ServantColonyMembership`，灵体移动位于 `Autonomy`，玩家 UI 位于 `Presentation`；不继续围绕 Guest 增加控制补丁。

以下是后续触碰对应模块时再处理的结构信号：

1. 无消费者的 `IServantAutonomyPolicy` 与旧 Guest 初始化服务已移除，旧存档 LordJob 类型仍保留。不要为未来敌方行为预建自治注册表。
2. 文档曾规划 `IPranaSource`，代码尚未建立通用魔力来源接口；新增第三种魔力来源时先确立来源契约，不继续向 `PranaCycleService` 堆分支。
3. `GameComponent_MoonWorld.warStartTick` 已从魔力文件分离，当前仍是公开字段，正常召唤通过幂等方法写入；本届事件的指定人与资格保存于 `currentWarEntry`，未预建阶段二状态机或多届生命周期。
4. `lordJobKey` 与 `targetPriorityKey` 当前没有运行时消费者。不得围绕字符串建立大型分支；等多策略需求出现后再改为真实 Def 或策略引用。
5. 构建脚本当前不是二进制确定性构建，相同源码重复编译仍会改变 DLL。以后可单独修正构建参数，不能把这种二进制变化误当作功能修改。

## 5. MVP 路线核对

结论：第一阶段 MVP、阶段二最小敌方交战、正式敌方突袭事件和场外休整再袭已确认可用。最小战争胜负已经验收，多阵营生命周期和多届战争尚未验收；不能把当前进度表述为整个 Mod 或战争玩法全部完成。

1. 已完成：召唤服务、契约、开战 Tick、御主/从者魔力、供魔、灵体、战败、自愈、生理规则、令咒两种效果、测试宝具、玩家成员与原版旅行接入。主从同队离图约束已按用户要求取消。
2. 正式接入已完成：中文剧本与常规存档“降灵之兆”调用同一事件接受流程，正式原版选点与调试入口调用同一召唤服务，不再依赖开发者批量赋予资格来完成正常开局。
3. 召唤范围已按用户最终规则统一：无己方单从者总数限制，本届事件只授予玩家指定的一人一次资格。旧设计中的“基地同时一骑”与“每人每届一次”均不适用；更多英灵的取得方式不在 MVP 内。
4. 顶部栏已随玩家成员迁移解决，不再作为独立待开发项。旧 Guest 设计、调试文案与操作指引有残留，触及开局/召唤文档时同步校正，不重新开发已验收机制。
5. 三阶索敌留到有实际敌方参与者时实现；玩家已获得实体控制权，自动索敌与玩家手动目标的优先关系需要在对应切片明确。
6. 正式英灵与全部宝具迁移继续延期：用户在另一 Mod 同步开发更多内容，待原型及 MoonWorld 本体开发进展适合时统一迁移；不再安排逐个 Excalibur 接入，也不把正式宝具内容作为当前 MVP 完成门槛。测试宝具继续验证本体结算。

当前路线：第一阶段 MVP（已验收） -> 阶段二最小敌对御主/从者组合（已验收） -> 正式敌方突袭事件（已验收） -> 场外休整/原从者再袭（已验收） -> 初始敌方工坊与两阵营胜负（已验收） -> 圣杯战争 Quest 与胜负历史归档（已验收） -> 简易工坊主动反击、独立撤离及撤离再战（已验收）。当前两阵营最小战争闭环已具备并通过上述验收；多届生命周期及完整七队仍不在当前实现。

## 6. 下一开发切片

当前已授权：在扩展计划切片 2 之前，先依赖接入 HolyGrailWarTest 已实现的七名从者，每职阶一名；不迁移资源、不修改原型、不提前实现七阵营运行。独立行动与跨图魔力切片已经用户确认，不重新列为待验收。

主动进攻和撤离再战已获用户明确确认，不再作为下一项待办。后续先评估 MVP 之后的扩展优先级；已验收机制仅在出现具体问题或相关代码变化时回归，不重复要求用户测试同一路径。

完整七队、多个工坊、敌方逃跑后新建据点、重新签约、多届战争和正式宝具迁移仍另行安排，不因当前闭环可玩就视为已实现。本轮收尾只更新验收进度与推送已验收提交，不开始新运行时代码开发。
## 7. 工作约束

- 优先复用 RimWorld 1.6 原版 Need、Hediff、Job、Lord、Ability、Quest 和 Gizmo 机制。
- 每个功能保持独立模块；Harmony 只作窄入口，不保存状态或承担完整业务流程。
- 不预建阶段二状态字段，不引入没有实际消费者的接口或注册表。
- 不覆盖 `HolyGrailWarTest`；当前作为内容依赖，后续正式迁移另行评估。
- 每次修改后运行 `Source\build.ps1 -Deploy`，同步到游戏 Mod 目录并由用户完成真实游戏验收。
- 未经用户确认，不新增硬依赖、不重写模块、不删除旧存档兼容类型。

