# MoonWorld 项目进度

> 本文件是项目进度的唯一真源。设计规则仍以 `docs/design` 下的设计文档为准；完成新切片后只在这里更新进度，不再新增第二份进度记录。

## 1. 当前基线

- 更新日期：2026-09-05
- 仓库：`D:\AGENT工作区\rimworld\MoonWorld`
- 分支：`main`
- 已验收提交：`163a4a4 Regenerate chronic servant conditions`
- 游戏目录：`G:\steam\steamapps\common\RimWorld`
- 测试部署目录：`G:\steam\steamapps\common\RimWorld\Mods\MoonWorld`
- 视觉与角色原型：`D:\AGENT工作区\rimworld\HolyGrailWarTest`
- 当前阶段：圣杯战争机制内核 MVP 已通过冒烟测试；正式召唤入口和实际英灵内容尚未迁入 MoonWorld，因此还不是完整的玩家可玩 MVP。

## 2. 已完成并验收

### 工程与数据边界

- 新建独立 Mod `MoonWorld`，没有覆盖 `HolyGrailWarTest`。
- 身份与静态数值使用 `ServantIdentityDef`、`ServantResourceProfileDef` 和 `ServantAutonomyProfileDef`。
- 契约关系和存在状态只由 `CompServantState` 保存。
- 御主与从者当前魔力分别由原版 Need 保存，不在组件中复制数值。
- 灵基受损和断供进度分别复用 Hediff severity 与 `ageTicks`。

### 御主魔力与供魔

- 魔术回路 Trait 通过 `DefModExtension` 引用回路 Def；魔术师位阶只作身份标签。
- 御主自然回魔、可调整供魔安全线、多从者均分和满魔退出均分池已经实现。
- 实体及灵体从者均可接受御主供魔。
- 从者原版自然饥饿下降关闭；实体化时按 10:1 将食物储备转化为魔力，并在原版进食边界停止。

### 从者身份、生理与自治

- 从者没有睡眠需求，不患疾病，不受衰老、寿命和饥饿归零惩罚。
- 从者保持原版 `Guest` 非殖民者身份，不能工作、征召或直接控制。
- 实体状态使用原版 `LordJob_DefendPoint` 满足长期需求，不含访客定时离图流程。
- 旧存档中的 `LordJob_ServantGuest` 会在载入时迁移；旧类暂时保留用于反序列化兼容。

### 灵体化与战败

- 主动灵体化由御主发起和解除；战败可进入战败灵体化。
- 灵体全部人物渲染层为 30% 不透明，普通目标无法索敌。
- 灵体只跟随御主，移动速度为原版速度的 300%；正常距离使用原版寻路，超过阈值时闪现追上。
- 灵体停止流血、不能攻击、工作或施放能力；不可站立格不能实体化。
- 普通伤害导致原版倒地或死亡时进入战败；直接 `Pawn.Kill` 不拦截。
- 战败时只最低限度修复致命状态，魔力清零并增加一级灵基受损；伤势仍会导致倒地或死亡时不能实体化。
- 御主死亡后，其未湮灭从者立即湮灭。

### 神秘度与自愈

- 当前白名单只允许实体化英灵造成的攻击伤害实体化英灵；环境伤害可以生效，直接处决不拦截。
- 普通伤口和疤痕按严重度逐点消耗维持线以上的魔力恢复。
- 背痛、虚弱等非伤口有害状态使用原版 `HealthUtility.Cure` 治愈，默认固定消耗 40 点维持线以上的魔力。
- `Hediff_MissingPart`、植入物和 MoonWorld 系统 Hediff 不进入肉体自愈候选。

## 3. 最近验收记录

- `059f66a Keep summoned servants on map`：修复从者按访客计时离图，加入旧存档 Lord 迁移。
- `163a4a4 Regenerate chronic servant conditions`：加入长期损伤治疗策略及 `conditionCurePranaCost=40`。
- RimWorld 1.6 编译通过。
- 游戏内冒烟测试通过，包括驻留、供魔、灵体化、战败、处决和长期损伤治疗。
- `main` 已推送到 GitHub；最后检查时工作树干净。

## 4. 当前模块化审查

本轮驻留与治疗实现可以验收，不需要重构：自治逻辑位于 `Autonomy`，治疗目标规则位于 `Prana/ServantHealingPolicy`，两者不共享可变状态，也不互相调用。

以下是后续触碰对应模块时再处理的结构信号：

1. `IServantAutonomyPolicy` 尚无真实实现者，当前使用单一静态自治服务；出现第二种自治策略时再建立真实策略分派。
2. 文档曾规划 `IPranaSource`，代码尚未建立通用魔力来源接口；新增第三种魔力来源时先确立来源契约，不继续向 `PranaCycleService` 堆分支。
3. `GameComponent_MoonWorld.warStartTick` 目前位于 `PranaCycle.cs` 且是公开字段。正式召唤接入时应保持类名和存档键兼容，将战争状态所有权从魔力模块分离，并提供幂等写入方法。
4. `lordJobKey` 与 `targetPriorityKey` 当前没有运行时消费者。不得围绕字符串建立大型分支；等多策略需求出现后再改为真实 Def 或策略引用。
5. 构建脚本当前不是二进制确定性构建，相同源码重复编译仍会改变 DLL。以后可单独修正构建参数，不能把这种二进制变化误当作功能修改。

## 5. 尚未完成的 MVP 内容

按当前优先级排列：

1. 正式召唤入口、阿尔托莉雅与卫宫内容迁移、成功召唤后记录 `warStartTick`。
2. 御主离开地图时必须与未湮灭契约从者处于同一离图队伍的生命周期约束。
3. 令咒计数与“奇迹重铸”。
4. 宝具迁移、统一魔力消耗入口以及令咒“宝具过载”。
5. 有实际敌方参与者后再实现英灵、御主、其他目标的三阶索敌。
6. 顶部小人栏灵体灰显等表现优化。

敌方御主与从者、据点、突袭、战争胜负和时间压力属于第二阶段战争闭环，不进入下一切片。

## 6. 下一开发切片

名称：正式召唤与战争启动。

目标：不用开发者菜单，玩家御主可以从当前可用池随机召唤阿尔托莉雅或卫宫；召唤、契约和战争开始形成一个成功后才提交状态的原子流程。

建议边界：

1. 保留 `GameComponent_MoonWorld` 的完整类型名和 `warStartTick` 存档键，将其从魔力文件中分离，并提供只写一次的战争启动接口。
2. 建立单一召唤服务，统一处理御主资格、地图与落点、单己方从者限制、候选池、Pawn 生成、契约绑定、失败回滚和开战记录。
3. 正式 Gizmo、任务入口和开发者按钮都只能调用该服务，不得各自复制生成与绑定流程。
4. 只从 `HolyGrailWarTest` 迁移阿尔托莉雅、卫宫所需的 Def、纹理、装备及最小角色初始化能力；不得整包复制原型代码。
5. 不得未经评估直接给 MoonWorld 增加 Humanoid Alien Races 等硬依赖。若保留现有外观确实必须新增依赖，应先向用户说明证据和替代方案。
6. 本切片不实现令咒、宝具、敌方据点、三阶索敌、完整离图规则或信赖控制。

验收：

- 玩家界面存在正式召唤命令。
- 有效御主可在有效落点随机召唤阿尔托莉雅或卫宫，外观和初始装备正确。
- 已有未湮灭己方从者时不能再次召唤。
- 生成或绑定失败时不留下 Pawn、契约或战争开始状态。
- 首次成功召唤只写入一次 `warStartTick`；保存读取后数值和契约保持不变。
- 开发者召唤入口与正式入口走同一服务。
- 编译、部署和新增冒烟测试通过；按独立可验收功能分批提交。

## 7. 工作约束

- 优先复用 RimWorld 1.6 原版 Need、Hediff、Job、Lord、Ability、Quest 和 Gizmo 机制。
- 每个功能保持独立模块；Harmony 只作窄入口，不保存状态或承担完整业务流程。
- 不预建阶段二状态字段，不引入没有实际消费者的接口或注册表。
- 不覆盖 `HolyGrailWarTest`，它只作为迁移来源和视觉原型。
- 每次修改后运行 `Source\build.ps1 -Deploy`，同步到游戏 Mod 目录并由用户完成真实游戏验收。
- 未经用户确认，不新增硬依赖、不重写模块、不删除旧存档兼容类型。

